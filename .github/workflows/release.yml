name: Release ZIP

on:
  release:
    types: [created]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: zip -r glitchsplash.zip glitchsplash/
      - uses: softprops/action-gh-release@v2
        with:
          files: glitchsplash.zip
